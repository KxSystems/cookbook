
This library provides a R server for Q.

See kx wiki https://code.kx.com/trac/wiki/Cookbook/IntegratingWithR

The Makefile works on l64 and l32. It requires that R be compiled as a shared object, libR.so.

Tested with R 2.7 and R 2.8.
